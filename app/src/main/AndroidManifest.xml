<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ingredientinspector">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.IngredientInspector">
        <activity android:name=".FoodInfoDisplay"></activity>
        <activity android:name=".FoodSearchListView" />
        <activity android:name=".InspectorSearch" />
        <activity android:name=".EditIngredientLabel" />
        <activity android:name=".IngredientListView" />
        <activity android:name=".IngredientLabeler" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>